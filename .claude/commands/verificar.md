---
description: Verifica el proyecto (type-check de Astro + build estático)
---
Verifica el proyecto ConfiaLine y reporta el resultado de forma clara:

1. Ejecuta `npm run check` (astro check / type-check de los `.astro`).
2. Ejecuta `npm run build` (build estático a `./dist/`).

Si algo falla, muestra el error exacto y propón el arreglo mínimo, pero **no apliques cambios sin confirmar**. Si todo pasa, dilo en una línea.
